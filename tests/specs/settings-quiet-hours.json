[
  {
    "name": "Configure quiet hours",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Choose when alerts should pause. Times are UTC."
            }
          }
        ],
        "send": {
          "callback": "settings:quiet_hours"
        }
      }
    ]
  }
]
