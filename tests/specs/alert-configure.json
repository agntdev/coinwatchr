[
  {
    "name": "Set alert rules",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No coins yet — add one before setting alerts."
            }
          }
        ],
        "send": {
          "callback": "alert:configure"
        }
      }
    ]
  }
]
